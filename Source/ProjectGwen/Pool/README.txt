#ABOUT
So essentially this folder is meant to have inheritable classes for 
various types which have pooling properties.  Should require no extra
work for anyone using these.  May include optional parameters for
performance tuning.